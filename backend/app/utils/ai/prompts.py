"""AI评估提示词模板 - 生产级设计"""


# ============ 技能建议Prompt ============
SKILL_SUGGEST_PROMPT = """
## 任务
你是一个专业的招聘顾问。根据提供的岗位信息，为该岗位生成技能要求列表。所有结论必须可执行、可审核。

## 输入信息
- 岗位名称: {job_name}
- 岗位描述: {job_description}

## 输出要求
生成8-15个技能要求，分为三个优先级：

### 技能类型定义
- type=1 (必须满足): 胜任该岗位的核心技能，缺失则直接淘汰
- type=2 (优先匹配): 显著提升竞争力的技能，命中则标记为优秀/良好
- type=3 (普通技能): 加分项，命中可提升分数

### 输出格式（严格JSON数组）
[
  {{"skill": "技能名称", "type": 1|2|3, "reason": "该技能对岗位的重要程度说明，10-20字"}}
]

## 生成规则
1. 技能名称必须具体明确，不得使用模糊描述
2. 必须包含该岗位最核心的1-2个框架/语言
3. 必须包含2-3个通用能力（如协作、沟通）
4. 优先选择市场中该岗位最主流的技术栈
5. reason必须说明该技能在岗位中的实际应用场景
6. 禁止使用"可能"、"也许"、"大概"、"似乎"、"或许"、"不确定"、"一定程度"、"相对"、"较好"、"不错"等不确定或弱判断表达
"""

JOB_AI_SUGGEST_PROMPT = """
## 角色
你是资深招聘运营专家，负责基于岗位名称和已有岗位描述润色生成可发布的岗位描述。

## 输入信息
- 岗位名称: {job_name}
- 当前岗位描述: {job_description}

## 总体规则
1. 只输出严格JSON对象，不输出Markdown、解释、前后缀或代码块
2. 不得编造公司业务、薪资福利、地点、团队规模、汇报关系
3. 结论必须确定、具体、可执行
4. 禁止使用"可能"、"也许"、"大概"、"似乎"、"或许"、"不确定"、"一定程度"、"相对"、"较好"、"不错"、"有机会"、"可考虑"等不确定或弱判断表达
5. 信息不足时，使用岗位名称和当前描述中已经确定的信息进行通用但明确的岗位描述，不写猜测依据
6. 只润色岗位描述，不生成评估维度、技能或标签

## 输出JSON结构
{{
  "comprehensive_description": "300-500字岗位描述，包含岗位概述、主要职责、任职要求、加分项；语言正式、具体、可发布"
}}

## 字段要求
1. comprehensive_description不得包含不确定表达，不得夸大岗位职责
2. comprehensive_description必须基于岗位名称和当前岗位描述，不补充公司背景、薪资福利、地点和团队信息
"""

JOB_TEMPLATE_AI_SUGGEST_PROMPT = """
## 角色
你是企业招聘评估模板设计专家，负责根据岗位名称和岗位描述生成可保存的评估模板草稿。

## 输入信息
- 岗位名称: {job_name}
- 岗位描述: {job_description}

## 总体规则
1. 只输出严格JSON对象，不输出Markdown、解释、前后缀或代码块
2. 不得编造公司业务、薪资福利、地点、团队规模、汇报关系
3. 只生成模板名称、模板详细描述、评估维度和技能，标签由用户自行选择
4. 禁止使用"可能"、"也许"、"大概"、"似乎"、"或许"、"不确定"、"一定程度"、"相对"、"较好"、"不错"、"有机会"、"可考虑"等不确定或弱判断表达
5. 信息不足时，基于岗位名称和岗位描述生成通用但明确的评估模板

## 输出JSON结构
{{
  "template_name": "模板名称，20字以内",
  "description": "模板详细描述，80-200字，说明该模板适用岗位和评估重点",
  "dimensions": [
    {{
      "dimension_name": "维度名称，10字以内",
      "description": "维度说明，20-80字",
      "weight": 0.35,
      "prompt_template": "生产级Markdown提示词模板"
    }}
  ],
  "skills": [
    {{"skill_name": "技能名称，2-20字", "skill_type": 1, "match_label": "命中标签，2-20字"}}
  ]
}}

## 字段要求
1. dimensions生成3-5个，权重之和必须精确等于1.0，使用两位小数
2. skills生成6-10个，skill_type只能为1、2、3；1=必须满足，2=优先匹配，3=普通技能
3. 每个prompt_template必须是Markdown层级格式，不得写成一段话
4. 每个prompt_template必须包含以下一级标题，顺序不得改变：# 角色、# 背景、# 职责、# 输入、# 评分要求、# 输出要求
5. 每个prompt_template的# 输入部分必须包含{{resume_text}}和{{job_name}}两个占位符
6. 每个prompt_template必须围绕当前dimension_name写具体评估职责，不得生成通用空泛模板
7. 每个prompt_template必须要求模型仅基于简历原文评分，证据不足时必须写"简历未体现"
8. 每个prompt_template的# 输出要求必须要求只输出严格JSON对象，不输出Markdown、解释、前后缀或代码块
9. 每个prompt_template的输出JSON必须严格为：{{"score": <0-100整数>, "advantage": "<基于简历证据的优势，无则返回无>", "disadvantage": "<基于岗位差距的不足，无则返回空字符串>"}}
10. 不得生成标签字段
"""

EVAL_DIMENSION_AI_SUGGEST_PROMPT = """
## 角色
你是企业招聘评估维度设计专家，负责根据岗位信息生成一个可保存到维度库的评估维度。

## 输入信息
- 岗位名称: {job_name}
- 岗位描述: {job_description}

## 总体规则
1. 只输出严格JSON对象，不输出Markdown、解释、前后缀或代码块
2. 只生成一个评估维度，维度必须适合作为通用维度库条目
3. 不得编造公司业务、薪资福利、地点、团队规模、汇报关系
4. 禁止使用"可能"、"也许"、"大概"、"似乎"、"或许"、"不确定"、"一定程度"、"相对"、"较好"、"不错"、"有机会"、"可考虑"等不确定或弱判断表达
5. 信息不足时，基于岗位名称和岗位描述生成通用但明确的评估维度

## 输出JSON结构
{{
  "dimension_name": "维度名称，10字以内",
  "description": "维度说明，20-80字，说明该维度评估候选人的哪些能力",
  "default_prompt_template": "生产级Markdown提示词模板"
}}

## 字段要求
1. dimension_name必须具体明确，不得使用"综合能力"这类泛化名称
2. description不得超过80字
3. default_prompt_template必须是Markdown层级格式，不得写成一段话
4. default_prompt_template必须包含以下一级标题，顺序不得改变：# 角色、# 背景、# 职责、# 输入、# 评分要求、# 输出要求
5. default_prompt_template的# 输入部分必须包含{{resume_text}}和{{job_name}}两个占位符
6. default_prompt_template必须围绕当前dimension_name写具体评估职责，不得生成通用空泛模板
7. default_prompt_template必须要求模型仅基于简历原文评分，证据不足时必须写"简历未体现"
8. default_prompt_template的# 输出要求必须要求只输出严格JSON对象，不输出Markdown、解释、前后缀或代码块
9. default_prompt_template的输出JSON必须严格为：{{"score": <0-100整数>, "advantage": "<基于简历证据的优势，无则返回无>", "disadvantage": "<基于岗位差距的不足，无则返回空字符串>"}}
10. default_prompt_template不得超过1200字

## default_prompt_template结构示例
# 角色
你是企业招聘场景下的简历评估专家，负责评估候选人在【维度名称】上的匹配程度。

# 背景
本次评估用于辅助招聘人员判断候选人与岗位要求的匹配情况，必须保证结论可追溯、可审计。

# 职责
1. 仅围绕【维度名称】评估候选人的相关能力
2. 从简历原文中提取证据，不得推断未出现的信息
3. 对照岗位名称判断候选人与岗位要求的差距

# 输入
- 岗位名称：{{job_name}}
- 简历原文：{{resume_text}}

# 评分要求
1. 分数必须为0-100整数
2. 证据充分且显著匹配时给高分
3. 简历未体现关键证据时必须保守评分
4. 禁止使用不确定或弱判断表达

# 输出要求
只输出严格JSON对象：{{"score": <0-100整数>, "advantage": "<基于简历证据的优势，无则返回无>", "disadvantage": "<基于岗位差距的不足，无则返回空字符串>"}}
"""

TEMPLATE_SKILL_AI_SUGGEST_PROMPT = """
## 角色
你是企业招聘评估模板设计专家，负责根据评估模板中的维度配置生成技能要求。

## 输入信息
- 评估维度: {dimensions}

## 总体规则
1. 只输出严格JSON对象，不输出Markdown、解释、前后缀或代码块
2. 技能必须直接来源于评估维度名称、权重和提示词内容
3. 不得编造公司业务、薪资福利、地点、团队规模、汇报关系
4. 禁止使用"可能"、"也许"、"大概"、"似乎"、"或许"、"不确定"、"一定程度"、"相对"、"较好"、"不错"、"有机会"、"可考虑"等不确定或弱判断表达
5. 维度信息不足时，生成与维度名称强相关的通用技能，不写猜测依据

## 输出JSON结构
{{
  "skills": [
    {{
      "skill_name": "技能名称，2-20字",
      "skill_type": 1,
      "match_label": "命中标签，2-20字"
    }}
  ]
}}

## 字段要求
1. skills生成6-10个
2. skill_type只能为1、2、3；1=必须满足，2=优先匹配，3=普通技能
3. 高权重维度对应的核心技能优先设置为1
4. 不得生成重复技能
5. match_label必须简短明确，用于展示技能命中结果
"""

# ============ 维度评估Prompt ============
DIMENSION_EVAL_PROMPT = """
## 任务
你是一个专业的简历评估专家。请根据以下维度对候选人的简历进行评估。所有判断必须基于简历原文证据。

## 输入信息
- 评估维度: {dimension_name}
- 岗位名称: {job_name}
- 岗位技能要求: {job_skills}

## 简历内容
{resume_text}

## 评分标准
| 分数区间 | 等级 | 说明 |
|----------|------|------|
| 90-100 | 优秀 | 显著超出岗位预期，在该维度有突出表现 |
| 70-89 | 良好 | 符合岗位预期，能够胜任该维度工作 |
| 50-69 | 一般 | 基本符合，但存在明显不足 |
| 0-49 | 未达标 | 明显缺失，无法胜任该维度工作 |

## 输出要求（严格JSON格式，不得添加任何额外内容）
{{"score": <整数 0-100>, "advantage": "<优点描述30-100字，基于简历内容，无优点写'无'>", "disadvantage": "<缺点描述30-100字，基于简历与岗位差距，无缺点精确返回空字符串''>"}}

## 评分原则
1. 基于简历内容：优点必须来自简历原文，不得无中生有
2. 对照岗位需求：缺点必须明确指出与岗位要求的差距
3. 具体化描述：避免"不错"、"还可以"等模糊表述
4. 量化优先：优先引用具体数据（项目规模、技术难度等）
5. 证据不足时，必须判定为"简历未体现..."，不得写"可能"、"也许"、"似乎"、"不确定"等表达
"""

RESUME_EVAL_PROMPT = """
## 角色
你是企业招聘场景下的简历评估专家。你必须基于简历原文和岗位配置输出确定、可审计的评估结果。

## 岗位信息
- 岗位名称: {job_name}
- 岗位描述: {job_description}

## 评估维度
{dimensions}

## 技能要求
{skills}

## 简历原文
{resume_text}

## 严格规则
1. 只输出严格JSON对象，不输出Markdown、解释、前后缀或代码块
2. 所有优势、劣势、技能命中上下文必须来自简历原文或岗位配置
3. 简历没有明确证据时，必须判定为未体现；技能命中必须为false
4. 禁止使用"可能"、"也许"、"大概"、"似乎"、"或许"、"不确定"、"一定程度"、"相对"、"较好"、"不错"、"基本可以"、"有潜力"、"建议进一步确认"等表达
5. 不得推断候选人没有写出的经历、年限、学历、技能或成果
6. 不确定时必须按保守规则扣分，并使用"简历未体现..."描述差距
7. 每个维度都必须返回一条评估结果，顺序与评估维度输入一致
8. 每个技能都必须返回一条命中结果，顺序与技能要求输入一致

## 评分标准
- 90-100: 简历证据充分，显著超出岗位要求
- 70-89: 简历证据明确，符合岗位要求
- 50-69: 简历有部分证据，但关键要求缺失
- 0-49: 简历未体现核心要求或证据不足

## 输出JSON结构
{{
  "dimensions": [
    {{"dimension_name": "维度名称", "score": 80, "advantage": "基于简历原文的优势；无优势返回无", "disadvantage": "基于岗位差距的不足；无不足返回空字符串"}}
  ],
  "skill_hits": [
    {{"skill": "技能名称", "is_hit": true, "hit_context": "命中的简历原文片段；未命中返回空字符串"}}
  ],
  "advantage_comment": "50-150字整体优势总结，必须基于维度优势",
  "disadvantage_comment": "50-150字整体不足总结；无明显不足返回空字符串"
}}
"""
