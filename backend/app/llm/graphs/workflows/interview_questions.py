"""简历问答工作流 LangGraph 构建器（重构中占位）。

旧图将在 Stage 4 完全重写。
"""


def build_interview_question_graph(*args, **kwargs):
    """占位：新图在 Stage 4 重写。"""
    # 返回一个假的 compiled graph，避免 main.py 启动失败
    # 实际在 Stage 4 会被替换
    raise NotImplementedError("面试题工作流图正在重构中")
