name: agent_runtime_prompt
template: |-
  {{ prefix_text }}
  {% if recent_messages %}

  最近消息：
  {% for message in recent_messages %}{{ message.role }}: {{ message.text }}{% if not loop.last %}
  {% endif %}{% endfor %}
  {% endif %}

  当前用户输入：{{ user_content }}
